/*=============================================== ButtonPill styles ===============================================*/

import styled from "styled-components"
import { Button, RADIUSES } from "@julseb-lib/react"

export const StyledButtonPill = styled(Button)`
    border-radius: ${RADIUSES.ROUND};
`
