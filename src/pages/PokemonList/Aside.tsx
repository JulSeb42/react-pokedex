/*=============================================== Aside PokéList ===============================================*/

import { AsideList } from "components"

export const AsidePokeList = () => {
    // TODO
    // ? Search pokemon (won't move before saving)
    // ? Search by type
    // ? 
    return <AsideList></AsideList>
}
