/*=============================================== {{>pascalName }} types ===============================================*/

export interface I{{>pascalName }} {
    {{#if children}}
    children?: Children
    {{/if}}
    {{#if as}}
    as?: ElementType
    {{/if}}
}
